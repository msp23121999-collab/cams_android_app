# ─── Secrets — NEVER commit ──────────────────────────────────────────────────
.env
.env.local
*.env
!.env.example
local.properties
!local.properties.example
secrets.properties

# Signing keys
*.jks
*.keystore
debug.keystore
keystore.properties

# ─── Reference web app — must never enter this repo ──────────────────────────
# The CAMS web app is a REFERENCE ONLY for schema/API contract.
# It is never copied into this deliverable.
cams-web-app/
repo_clone/
frontend/
*.tsx
*.jsx
node_modules/
package-lock.json
vite.config.*

# ─── Android build output ────────────────────────────────────────────────────
build/
.gradle/
.cxx/
*.apk
*.aab
*.ap_
*.dex
captures/
.externalNativeBuild/

# ─── Python / backend ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/

# Local database files and dumps — schema lives in backend/alembic/
*.db
*.db.bak*
*.db.backup*
*.sqlite
*.sqlite3
backups/

# Uploaded user files (runtime data, not source)
backend/app/static/uploads/

# ─── IDE / OS ────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# ─── Scratch / logs ──────────────────────────────────────────────────────────
*.log
crash*.txt
scratch*
