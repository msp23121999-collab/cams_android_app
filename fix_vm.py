with open("app/src/main/java/com/example/features/student/providers/DashboardViewModel.kt", "r") as f:
    lines = f.readlines()
    
# We want to remove line 75 and 76 or whatever is extra.
# Let's just fix it properly.
